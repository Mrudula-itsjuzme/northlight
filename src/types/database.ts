/**
 * Placeholder Supabase generated-types shape. Once a real Supabase project
 * exists, replace this with the output of:
 *
 *   npx supabase gen types typescript --project-id <project-ref> > src/types/database.ts
 *
 * Until then, application code should rely on the Drizzle schema
 * (`src/db/schema.ts`) as the source of truth for table shapes, and this
 * type is intentionally loose so the Supabase client compiles.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
