// Test-only stub for the `server-only` marker package (see vitest.config.ts
// for why this alias exists). Intentionally empty: importing this module
// must be a no-op, exactly like Next.js's own "react-server" resolution of
// the real package.
export {};
